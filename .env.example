# ===================================================================
#          AI TV 추천 시스템: 환경 변수 설정 예시 파일
# ===================================================================
#
# 사용 방법:
# 1. 이 파일의 이름을 '.env'로 복사합니다.
# 2. 각 변수의 값('<...>')을 자신의 환경에 맞게 수정합니다.
# 3. 이 파일은 절대로 Git 저장소에 커밋하면 안 됩니다. (.gitignore에 포함되어 있습니다)
#

# --- 애플리케이션 환경 ---
# 애플리케이션 실행 환경을 지정합니다. (e.g., development, production, test)
APP_ENV="development"

# --- Google Gemini API ---
# LLM 기반 콘텐츠 라벨링에 사용되는 Gemini API 키입니다.
# Google AI Studio (https://aistudio.google.com/app/apikey)에서 발급받으세요.
GEMINI_API_KEY="<your_google_gemini_api_key_here>"


# --- 데이터베이스 (MySQL) ---
# docker-compose.yml에 설정된 DB 서비스 정보와 일치해야 합니다.
# 애플리케이션(src)이 데이터베이스에 연결하기 위해 이 정보를 사용합니다.
DB_CONNECTION="mysql+pymysql"
DB_HOST="db"  # Docker Compose 내부 네트워크에서 사용하는 서비스 이름
DB_PORT="3307"
DB_DATABASE="tv_recommendation_db" # docker-compose.yml의 MYSQL_DATABASE와 일치
DB_USER="app_user"                 # docker-compose.yml의 MYSQL_USER와 일치
DB_PASSWORD="app_password"         # docker-compose.yml의 MYSQL_PASSWORD와 일치
# DB_ROOT_PASSWORD는 docker-compose에서만 사용되므로 애플리케이션은 알 필요가 없습니다.


# --- AI/ML 모델 설정 ---
# Sentence Transformer 임베딩 모델 이름입니다.
# Hugging Face Hub (https://huggingface.co/models)에서 원하는 모델로 변경할 수 있습니다.
EMBEDDING_MODEL_NAME="models/text-embedding-004"


# --- 웹 크롤러 설정 ---
# IPTV 편성표를 크롤링할 대상 URL입니다.
CRAWL_TARGET_URL="https://www.lguplus.com/iptv/channel-guide/schedule"


# --- Airflow 설정 ---
# Airflow 스케줄러 및 웹서버 실행을 위한 사용자 ID입니다.
# 로컬 파일 시스템의 권한 문제를 방지하기 위해 사용됩니다.
# 터미널에서 `id -u` 명령어로 현재 사용자 ID를 확인할 수 있습니다.
AIRFLOW_UID=50001